services:
  chatapp:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - .:/app
    environment:
      FLASK_ENV: production
      FLASK_APP: minimal_webserver.py
    command: ["python", "minimal_webserver.py"]